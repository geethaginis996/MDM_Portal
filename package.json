{
  "name": "mdm-portal",
  "version": "1.0.0",
  "description": "MDM Portal CAP Application",
  "main": "srv/server.js",
  "scripts": {
    "start": "cds run",
    "dev": "cds watch --exclude db/*.sqlite*,db/*.db,*.log",
    "build": "cds build --production"
  },
  "keywords": [],
  "author": "",
  "license": "UNLICENSED",
  "dependencies": {
    "@sap/cds": "^9.9.1",
    "express": "^4.18.2",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "@sap/cds-dk": "latest",
    "@cap-js/sqlite": "^2"
  },
  "cds": {
    "requires": {
      "[development]": {
        "db": "sqlite"
      }
    }
  }
}